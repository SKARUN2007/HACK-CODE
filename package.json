{
  "name": "makkalsaantru-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "MakkalSaantru - Citizen-powered, AI-assisted public-work verification platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm run build:server && npm run build:client",
    "seed:demo": "npm run seed:demo --prefix server",
    "test": "npm test --prefix server",
    "scan:secrets": "node scripts/scanSecrets.js"
  },
  "keywords": [
    "civic-tech",
    "public-works",
    "ai-verification",
    "pwa",
    "makkalsaantru"
  ],
  "author": "MakkalSaantru Team",
  "license": "MIT"
}
